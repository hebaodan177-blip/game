# 忧郁消消乐 · 治愈机制优化分析报告

> 目标：在不破坏现有静态网页功能的前提下，解决「安慰治愈行为过于单一」的问题。
> 方法：代码走查 + 运行时冒烟测试 → 结构诊断 → 多元化方案评估（可行性 / 成本 / 风险）。

---

## 一、现状分析与测试结论

### 1.1 代码结构基线（当前版本）

| 文件 | 职责 | 与治愈机制的关系 |
|---|---|---|
| `data.js` | 常量、关卡、文案、存档工具 | 情绪阶段文案、存档读写 |
| `board.js` | 棋盘匹配/消除/掉落 | 纯逻辑，与情绪无关 |
| `renderers.js` | 粒子/棋盘/角色渲染 | 角色姿态随情绪阶段变化 |
| `audio.js` | Web Audio 程序化音效 | 情绪阶段驱动雨声/心跳强度 |
| `ui.js` | EmotionSystem / 文本 / 剧情 / UI | **情绪数值核心** |
| `game.js` | 主控制器 | **治愈逻辑的唯一入口** |
| `music.js` | 音乐播放器 | 与情绪无耦合 |
| `assets.js` | 外部资源加载 | 与情绪无耦合 |

### 1.2 运行时冒烟测试结果（基线验证）

```
PASS game constructed              PASS 5 piece images loaded
PASS hud updated                   PASS rain clear healed emotion
PASS score increased               PASS non-rain clear leaves emotion unchanged
PASS invalid swap rolled back      PASS save persisted
PASS map has 5 levels
```

9 项全部通过，现有功能完好，可作为后续改动的回归基线。

### 1.3 治愈单调的代码证据

全项目 `emotion.change()` 仅有 **2 处调用**：

| 位置 | 触发条件 | 效果 |
|---|---|---|
| `game.js:232` | 消除**雨滴**时 | `change(rain × 2.8 + 连锁加成)`（唯一正向治愈） |
| `game.js:315` | 挂机 > 5s | `change(-0.25)`（唯一负向衰减） |

结论：**治愈通道 = 消雨滴回血**，一条路径、恒定数值。

---

## 二、治愈单调的结构性根源

1. **通道唯一**：`leaf / cloud / moon / mist` 四类棋子只贡献分数，完全不触达情绪（已被测试证实）。治愈与「收集雨滴」这一关卡目标耦合，第一章之外缺少独立安抚入口。
2. **数值恒定**：治愈量与情绪阶段无关。`score` 有阶段系数（`game.js:230` 的 `mult`），`emotion` 却没有——低潮时治愈既不更稀缺也不更珍贵。
3. **无事件总线**：`EmotionSystem` 只是一个「数值 + 阈值映射」，没有"安抚事件"的概念，任何新治愈形式都需要自己写状态机。
4. **无渐进感**：治愈是即时加减法，没有"积累 → 回报"的节奏；低潮 → 回升的戏剧性时刻没有任何仪式化表现。
5. **无差异化反馈**：所有棋子消除共用同一音效与 30% 概率的随机独白，情绪反馈缺少与"内容"的对应关系。

---

## 三、多元化治愈方案与评估

### A 组 · 新增安抚互动（不同形式的互动入口）

#### A1. 点击角色 · 主动安抚（推荐 ★★★★★）

**设计**：点击侧栏「窗边的人」→ 角色回望/抬手（新姿态）→ 温柔独白一句 → 情绪 +2~3，20 秒冷却防止连点刷数值。

**改动点**：
- `game.js` `bind()`：给 `this.character.canvas` 绑 `pointerdown`（沿用 `input()` 现有的 screen-guard 模式）
- `renderers.js`：`POSES` 增加 1 个姿态条目（现有插值系统自动支持，零额外框架代码）
- `ui.js` / `data.js`：安抚台词表 + 冷却时间戳字段

**可行性**：极高。角色已有完整的 pose 补间系统，加姿态 = 加一行配置；输入已有 pointer 处理先例。
**实现成本**：低（约 30 行）。
**风险**：**低**。
- ⚠ 唯一坑：`.character-copy`（角色卡片上的文字层）默认会拦截点击，需 CSS 加 `pointer-events: none`；
- ⚠ 需要像 `input()` 一样在 `mapScreen/introScreen/resultScreen` 打开时忽略点击，避免弹层下误触发。

#### A2. 撑伞护盾 · 阶段性强安抚（推荐 ★★★★）

**设计**：侧栏新增「撑伞」按钮，每章 2 次；开启后 10s 内挂机情绪不再衰减、雨粒子密度下降、角色移至窗前（pose G），结束自动复原。

**改动点**：`index.html` +按钮；`game.js` 护盾状态机（开启/倒计时/结束复位）；`renderers.js` `ParticleSystem` 加临时低密度接口；`audio.js` 可选"伞下闷音"滤波。

**可行性**：高。粒子密度目前由 `setStage()` 管理，加一个临时覆盖即可。
**实现成本**：中（约 60~80 行）。
**风险**：**中低**。
- ⚠ **最大风险是"状态未复位"**：护盾结束必须调用 `applyStage(this.emotion.stage)` 恢复粒子/音频，否则雨会永久变小；
- 规避：复位统一走 `applyStage`，护盾状态与全局 stage 分离存储（`shieldUntil` 时间戳，tick 里检查过期即复位）。

#### A3. 跨区拖拽棋子到角色（本期不做）

**设计**：把棋子拖出棋盘交给角色，换取大额安抚。
**实现成本**：高（需改 `input()/swap()` 核心交互链 + 跨 canvas 命中测试）。
**风险**：**高**（直接触碰核心输入流程）。
**结论**：性价比低，不建议本期实施。

---

### B 组 · 丰富情感反馈机制（同一交互的差异化反馈）

#### B1. 各棋子差异化治愈反馈（推荐 ★★★★★）

**设计**：每种棋子消除给不同"情绪调性"的反馈，全部**只加内容、不动数值**：

| 棋子 | 音效调性 | 示例独白 |
|---|---|---|
| 雨滴 rain | 温柔升调（现状保留） | 「雨水落回你心里，轻了一点」 |
| 枯叶 leaf | 怅然低音 | 「有些告别落下来，也很好」 |
| 残月 moon | 清冷释然 | 「残缺，也是一种完整」 |
| 灰雾 mist | 朦胧混响 | 「看不清也没关系，慢慢走」 |
| 乌云 cloud | 压抑闷响 | 「云会散的，你也会」 |

**改动点**：`audio.js` `clear()` 增加 type 参数按类型选音色；`data.js` 加消除文案表；`game.js` `resolve()` 从文案表取（替换现有 30% 随机独白）。
**实现成本**：低（约 40 行，内容型）。
**风险**：**极低**（纯新增，不动机制）。
**额外收益**：为 B3 的"治愈画像"埋下统计钩子。

#### B2. 情绪联动数值 · 低谷治愈更有力（推荐 ★★★★）

**设计**：
- 治愈量随阶段上调：阶段 1–2 = 现状 2.8/滴；阶段 3（深渊）= 3.6；阶段 4（绝望）= 4.4 —— "越需要安抚，安抚越有效"；
- 挂机衰减随阶段放缓（低潮时不落井下石）。

**改动点**：`game.js:232` 的 `emotion.change` 参数加阶段系数（`score` 已有 `mult` 先例，一行照搬）；`game.js:315` 的 `-0.25` 调整。
**实现成本**：**极低**（2 处数值）。
**风险**：**极低**。
- ⚠ 注意关卡平衡：第 5 章 survive 通关要求 `emotion ≥ 40`，衰减放缓会降低难度，需同步验证。

#### B3. 治愈画像统计（推荐 ★★★）

**设计**：统计各棋子累计消除数形成"治愈倾向"，结算与回忆日志中动态呈现（如「你靠雨滴 40 次、枯叶 12 次，一点点愈合」）。
**改动点**：`game.js` 加统计对象 + `resolve()` 累计；`data.js`/`ui.js` 结算文案；存档加字段。
**实现成本**：低（约 30 行）。
**风险**：**低**；存档兼容见第五节（需默认值兜底）。

#### B4. 角色表情分级（推荐 ★★★）

**设计**：`CharacterRenderer` 按情绪阶段插值眉角/嘴角，让"窗边的人"从紧锁到舒展有表情变化。
**改动点**：`renderers.js` 加表情参数并在 `frame()` 绘制。
**实现成本**：中（Canvas 绘制约 50 行）。
**风险**：**低**（独立绘制区域，不触碰其他系统）。

---

### C 组 · 渐进式治愈（节奏与成长感）

#### C1. 暖意蓄条 · 积累式治愈（推荐 ★★★★，核心机制）

**设计**：新增「暖意」蓄条（放情绪卡片内）：消除棋子积累暖意（雨滴 +3 / 其他 +1），**蓄满触发一次"被安慰"仪式**——柔光闪烁 + 专属音效 + 角色放松姿态 + 独白 + 情绪 +6。让治愈从"即时小额"变成"累积大额 + 仪式感"。

**改动点**：
- `game.js`：`warmth / warmthMax` 字段，`resolve()` 累积，蓄满置旗；
- 触发点放在 `resolve()` 返回、`busy=false` 之后（`check()` 前），**不插入现有动画时序**；
- `index.html` + `styles.css`：情绪卡加一条暖意进度；
- 复用现有 `toast()/audio.tone()/particles.emit()`，无需新框架。

**实现成本**：中（约 50~60 行）。
**风险**：**中低**。
- ⚠ 新 DOM 元素必须存在性校验（`UIManager.$` 直接 `querySelector`，缺失会抛错）；
- ⚠ 触发不得阻塞 `resolve()` 的 while 循环动画流，故置于循环外。

#### C2. 雨停彩蛋 · 情绪回升的仪式感（推荐 ★★★）

**设计**：当情绪从深渊/绝望（3/4 阶段）回升到忧郁/平静（≤2）时，触发"雨停 4 秒"：粒子密度骤降 + 一束光 + 独白「天，好像亮了一点」。
**改动点**：`renderers.js` `ParticleSystem` 加 `calm(ms)` 临时状态（与 A2 共用复位路径）；`game.js` `stageChange()` 检测回升方向时触发。
**实现成本**：中（约 40 行）。
**风险**：**中低**（粒子状态复位风险，与 A2 相同规避方式——统一走 `applyStage` 复位）。

#### C3. 失败不惩罚 · 安慰收尾（推荐 ★★★★，零成本）

**设计**：现有失败结算文案已较温柔，补充：失败后情绪不再恶化 + 结算屏追加一句角色安慰（如「没关系的，雨总会停一次」）。
**改动点**：`game.js` `result(false)` 文案；可顺手加一次小额暖意补给。
**实现成本**：**极低**（1~2 行文案）。
**风险**：**无**。

---

## 四、推荐实施顺序

| 优先级 | 方案 | 成本 | 风险 | 理由 |
|---|---|---|---|---|
| P0 | B1 差异化反馈 / B2 阶段联动 / C3 失败安抚 | 极低 | 极低 | 内容型改动，立刻提升反馈密度 |
| P1 | A1 点击安抚 / C1 暖意蓄条 | 低~中 | 低 | 打开"主动安抚 + 渐进治愈"两个新维度，性价比最高 |
| P2 | B3 治愈画像 / C2 雨停彩蛋 / B4 表情分级 | 中 | 中低 | 增强长期留存与仪式感 |
| P3 | A3 跨区拖拽 | 高 | 高 | 本期不做 |

建议落地顺序：**P0 全做 → P1 的 A1 → C1 → P2 按需**。每一步完成可运行一次冒烟测试回归（本报告第一节的 9 项即可覆盖）。

---

## 五、破坏静态网页的通用风险与规避

| # | 风险 | 场景 | 规避方式 |
|---|---|---|---|
| 1 | **DOM 缺失报错** | `UIManager.$` 直接 `querySelector`，新增元素若未同步加入 HTML，`textContent` 赋值即抛错，**整局中断** | 新增 DOM 与绑定一次性完成；或新逻辑用存在性检查 `el ? ... : ...` |
| 2 | **脚本依赖顺序** | 新类若使用 `data.js` 的常量/工具（`choice/clamp/wait`），加载顺序错了会 `undefined` | 沿用现有顺序 `assets→data→board→renderers→audio→ui→music→game`；新代码放 `game.js` 内或按序插入 |
| 3 | **存档兼容** | 旧存档缺新字段（如治愈统计），直接读取得到 `undefined` | 沿用 `loadProgress()` 现有模式：读取时 `Number.isInteger`/默认值兜底，再 `clamp` |
| 4 | **静态托管约束** | 项目部署于 GitHub Pages / `file://`，无构建、无后端 | 所有方案仅用 Canvas / DOM / Web Audio / 原生 JS，符合约束；不要引入 npm 依赖或需服务的 API |
| 5 | **性能** | 新增持续动画若自建 rAF 循环，会多一条 60fps 渲染线 | 新动画并入 `renderers.js` 现有 `frame()` 循环；粒子临时状态复用 `ParticleSystem` 既有接口 |
| 6 | **音频自动播放** | 浏览器禁止无声誉的自动播放，独立 `AudioContext` 会被 block | 所有新音效经 `AudioManager.tone()`（由 `unlock()` 统一管理），不新增独立 Context |
| 7 | **指针事件遮挡** | 新交互区被文字/装饰层拦截（如 A1 的 `character-copy`） | 被遮挡层加 `pointer-events: none`；交互触发处复制 `input()` 的屏幕守卫条件 |
| 8 | **状态未复位** | 护盾/彩蛋类临时状态若异常退出，画面永久异常 | 统一以 `applyStage(stage)` 为复位唯一入口；临时状态用时间戳 + `tick()` 检测过期，不做多层 flag |

**最重要的两条红线**：① 任何新 DOM 必须经过**存在性检查**（红 #1）；② 任何临时视觉状态必须**单一复位路径**（红 #8）。守住这两条，以上方案均不会破坏现有功能。

---

## 六、附带发现（非治愈主题，顺带提醒）

1. **`music/manifest.json` 存在重复曲目**：「周杰伦-晴天.mp3」与「周杰伦-晴天 (2).mp3」字节数相同（10792943），重复加载会占用播放列表，建议删除其一并更新清单。
2. **版权风险**：README 已声明"只发布自己拥有使用权的音频文件"，但 manifest 仍登记了受版权保护的曲目。若公开部署到 GitHub Pages，存在侵权风险，建议仅保留自有版权音乐。
3. **README 提及 `.tmp-server.js`**：该文件仅本地预览用，当前工作区已不存在；README 描述已说明不依赖它，无需处理。

---

*测试基线：9 项冒烟断言全部 PASS · 8 个 JS 文件 `node --check` 语法通过 · 冒烟脚本已清理*
