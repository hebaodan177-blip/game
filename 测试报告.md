# 视觉与渲染优化全面测试报告

测试日期：2026-09-01  
测试对象：`index.html`、`renderers.js`、`styles.css`、`assets.js` 及其静态资源  
基准文档：`视觉与渲染优化分析.md`

## 1. 执行摘要

代码和静态资源检查结果良好：`renderers.js`、`board.js`、`game.js`、`ui.js`、`assets.js`、`data.js`、`audio.js`、`music.js` 均通过 `node --check`，本地静态服务器返回 HTML 状态 200。渲染层已接入分层背景、预渲染棋子、DPR 上限、质量分级和 reduced-motion 降级。

真实浏览器截图、FPS、长任务、跨浏览器和触控手势测试未能执行：浏览器控制运行时在初始化阶段返回 `helper_unknown_error`，当前环境也未发现可直接调用的 Chrome/Edge 可执行文件。因此，视觉还原度和运行时性能结论中，标记为“代码证据”或“待实测”，不将静态推断冒充实测。

## 2. 测试矩阵与结果

| 测试项 | 对应优化条目 | 方法/证据 | 结果 |
|---|---|---|---|
| 背景情绪色调 | 3.1 SKY 情绪映射 | 检查 `SKY` 五阶段及 `ParticleSystem.setStage()` | 通过（代码证据）：五组 top/mid/bottom/glow/fog 均存在，阶段切换会重建雨幕 |
| 远景层次 | 3.1 分层远景 | 检查 hills/city/windows/ground/stars 绘制调用 | 通过（代码证据）：三层山影、城市楼体、窗灯、地面反光和星光均在主循环绘制 |
| 多层雨幕/水花/玻璃水滴/灰烬 | 3.1 粒子系统 | 检查 far/mid/near、splash、glass、ash 数组及回收 | 通过（代码证据）：三层雨丝，水花、玻璃轨迹和灰烬均有生命周期回收 |
| 棋盘玻璃材质 | 3.2 棋盘底板材质 | 检查 `drawBoardBackground` 的径向渐变、边缘/顶部高光 | 通过（代码证据） |
| 棋子材质与辨识度 | 3.2 `PIECE_PALETTE`/`paintPiece` | 检查五类程序化绘制和 `buildSprites()` | 通过（代码证据）：五类颜色参数齐全，运行时使用 `drawImage` 精灵 |
| 下落/选中/消除动效 | 3.2 动效增强 | 检查 `easeBack`、`dropAnim`、`clearing`、`drawSpecial` | 通过（代码证据）；实际观感待浏览器实测 |
| 角色与室内场景 | 3.3 CharacterRenderer | 检查 POSES、窗户/窗框/雨痕/冷光绘制和 `play/setPose` | 通过（代码证据） |
| 后期滤色与暗角 | 3.4 styles.css | 检查 vignette、stage filter、面板/按钮玻璃和扫光 | 通过（代码证据） |
| 背景照片纹理 | 3.4 / assets.js | `assets/backdrop.png` 加载逻辑及 `fadeIn` | 部分通过：资源会注入 `#backdropImage`，动画终点为 0.5；初始 2.4 秒 opacity 为 0，需确认是否符合预期 |
| 响应式布局 | 3.4 响应式 | 检查 920px/600px media query | 代码通过：桌面侧栏、平板双列、移动单列均有规则；实际溢出/点击区域待实测 |
| reduced-motion | 4 性能保障 | 检查 JS `matchMedia` 和 CSS media query | 通过（代码证据）：动画/过渡压缩，粒子质量降为 low 并冻结移动 |
| DPR 上限 | 4 DPR 上限 | 检查 Particle/Board/Character resize | 部分通过：三个渲染器均 `min(devicePixelRatio, 2)` |
| 粒子预算分级 | 4 粒子预算分级 | 检查 `detectQuality()` 及各粒子数量 | 部分通过：CPU 核心和 coarse pointer 生效；DPR 条件存在逻辑缺陷（见问题 P1） |
| 精灵预渲染 | 4 棋子预渲染精灵 | 检查 `buildSprites` 与 `drawImage` | 通过（代码证据） |
| 页面加载速度 | 4 / 资源策略 | 本地 HTTP 计时与文件体积 | HTML 首次约 197 ms；重复请求约 3–12 ms（本机 localhost，非真实网络） |
| JS 语法/启动阻断 | 工程回归 | `node --check` 8 个 JS 文件 | 通过 |
| 跨浏览器兼容性 | 全部 | 目标 Chrome/Edge/Firefox/Safari | 未执行：缺少可用浏览器自动化运行时；需在 CI/真实设备补测 |
| 优化前后对比 | 全部 | 仓库未提供优化前基线提交/截图/性能记录 | 未执行：无法量化像素差异、FPS 或加载收益 |

## 3. 性能数据

### 3.1 资源体积

| 资源 | 大小 |
|---|---:|
| `assets/backdrop.png` | 1,621,473 B（约 1.55 MiB） |
| 4 首 MP3 | 38,303,597 B（约 36.5 MiB） |
| 关键首屏代码（HTML + CSS + renderers.js） | 75,933 B（约 74.2 KiB） |
| 项目文件总量（含音乐） | 40,092,196 B（约 38.2 MiB） |

音乐文件不应在首屏预加载；需通过 Network 面板确认 `music.js` 是否只在用户选择/播放时发起请求。

### 3.2 代码级运行时风险

- 背景和棋盘各自持续 `requestAnimationFrame`；CharacterRenderer 另有一条 RAF。高分辨率设备上仍会产生三条持续绘制循环。
- `drawBoardBackground()` 每帧创建多个渐变；城市窗口和雾层也会在每帧生成渐变。精灵优化降低了棋子路径成本，但未消除背景渐变分配。
- `bits`、`splash` 通过 `filter` 回收，存在每帧创建新数组的分配压力；文档所述“对象池”并未覆盖这两类粒子。

## 4. 发现的问题与优先级

### P1：DPR 质量分级条件永远不会触发

位置：`renderers.js` 约 54、83、90 行。构造函数和 `resize()` 先将 `this.dpr` 截断到最大 2，随后 `detectQuality()` 再判断 `this.dpr > 2`。因此该条件恒为 false，高 DPR 触屏设备可能被判定为 high（若 CPU 核心数大于 4 且非 coarse pointer），与分析文档“按 DPR 分级”的目标不一致。

建议：保留原始 DPR（例如 `const rawDpr = window.devicePixelRatio || 1`）供 `detectQuality()` 使用，再将实际画布 DPR 截断到 2；增加 DPR 3/4、触屏和低核数的单元测试。

### P2：背景图初始透明，纹理延迟 2.4 秒才显现

位置：`styles.css:65-71`。`#backdropImage` 的 CSS 初始 `opacity: 0`，`fadeIn` 结束才到 0.5。若目标是首屏即有照片纹理，这会造成前 2.4 秒与设计稿不一致；若目标是避免突兀，则应在测试标准中明确该延迟。

建议：将其定义为有意的渐入并在视觉验收中记录；或改为资源加载完成后立即以较低透明度显示。

### P2：缺少可量化的优化前基线

当前仓库没有优化前版本、截图、Lighthouse 或 FPS 记录，无法验证“优化前后”收益。建议保留基线 commit，并在相同 DPR、视口、网络节流和章节状态下采集 LCP、INP、CLS、平均/1% low FPS、内存和总传输量。

### P2：持续全屏 Canvas 绘制可能影响移动端功耗

主循环每帧重绘整张背景和特效画布；窗口不可见时也未见 Page Visibility 暂停逻辑。建议在 `document.visibilityState !== 'visible'` 时暂停或降频，并在移动设备上实测 5 分钟功耗/温升。

### P3：外部字体依赖增加网络不确定性

`assets.js` 注入 jsDelivr 字体样式表，离线、受限网络或中国大陆网络环境下可能回退字体，造成排版和视觉差异。建议提供本地字体子集或明确字体回退验收标准。

## 5. 必须补做的真实浏览器测试

1. Chrome/Edge/Firefox/Safari 最新稳定版：首屏截图、章节切换、交换/消除/特殊棋、音乐面板、键盘焦点和错误日志。
2. 视口 1440×900、1024×768、768×1024、390×844、320×568：检查棋盘是否变形、文字溢出、按钮最小触控尺寸和滚动锁定。
3. 性能：CPU 4 核/DPR 1、CPU 8 核/DPR 2、DPR 3 触屏三组；记录 60 秒平均 FPS、P95 帧时间、长任务、JS 堆、GPU 内存和电池消耗。
4. `prefers-reduced-motion: reduce`：确认雨幕/灰烬/漂浮/按钮动画确实停止或降级，且交互反馈仍清晰。
5. 网络：Fast 3G/离线/字体 CDN 失败/背景图失败；确认程序化背景和系统字体回退正常。
6. 建立优化前基线后，使用相同脚本和环境输出差异表，避免只凭主观观感判定优化有效。

## 6. 结论

本次静态和本地 HTTP 验证表明，文档中列出的主要视觉渲染能力已落地，代码无语法阻断，基础资源可由静态服务器正常返回。当前不能对像素还原度、真实 FPS、加载核心指标、跨浏览器兼容性和优化前后收益做最终签字；在修复 DPR 分级逻辑并完成真实浏览器矩阵、性能基线和对比截图前，建议将验收状态定为“代码级通过，运行时验收待补”。


