# 消除元素配色与音效设计

> 适用范围：`renderers.js`（`PIECE_PALETTE` 与棋子绘制）、`audio.js`（`AudioManager.clear` 与各元素音色）
> 原则：**高区分度 + 克制低沉**。配色与音效统一服务游戏忧郁、低沉的视觉主题。

## 1. 设计目标

1. 不同元素在快速操作下一眼可辨：色相轴拉开，明度/饱和度拉开。
2. 整体克制：饱和度控制在中低档，避免鲜艳刺眼，贴合"雨、记忆、未说完的话"的基调。
3. 消除音效与视觉统一：视觉越暗沉 → 音高越低；视觉越清冷 → 质感越脆/气声化；音量整体偏低。

## 2. 配色方案

五种元素分布在一条"冷 → 暖"的色相轴上，并留出唯一高亮色（残月），保证任意两种元素都可区分。

| 元素 | 主色 | 深色（暗部） | 高光 | 轮廓光 | 色相定位 | 适用场景 |
|---|---|---|---|---|---|---|
| 雨滴 `rain` | `#6096A8` 青蓝 | `#264858` | `#CAE8F0` 冰蓝 | `#8EC0CE` | 冷（H≈195°） | 开局雨章、水的质感、清爽冷冽 |
| 枯叶 `leaf` | `#967452` 赭褐 | `#4C3824` | `#CAB08C` 枯黄 | `#AC9068` | 暖（H≈33°） | 叶章、凋零暖意、与冷色形成对比 |
| 乌云 `cloud` | `#58607E` 靛紫灰 | `#2A2F46` | `#9CA4C4` 云隙亮 | `#7E88AC` | 冷紫（H≈245°） | 云章、阴沉压抑、最深冷色 |
| 残月 `moon` | `#C6B89C` 暖米 | `#80765E` 月影 | `#F0E8D4` 月辉 | `#E2D2AC` | 暖（H≈42°，L≈69%） | 月章、唯一的亮色聚焦、回忆与夜 |
| 灰雾 `mist` | `#6C747A` 冷灰 | `#3C4248` | `#AAB2B8` | `#8E969E` | 无彩（S≈6%） | 雾章、迷离混沌、最低饱和 |

### 区分度说明

- **雨 vs 云**：青蓝（有彩、较亮）与靛紫灰（偏紫、更深）色相差约 50°。
- **雨 vs 雾**：青蓝有彩 vs 灰雾无彩（饱和度差异显著）。
- **叶 vs 月**：同为暖色系，但叶暗沉（L≈46%）vs 月明亮（L≈69%），明度差大。
- **云 vs 雾**：云带紫色相 vs 雾纯灰，饱和度与明度都有差异。
- **月** 是全场唯一高亮色，棋盘上天然成为视觉焦点。

### 落地位置

- 主色 `base` / 深色 `dark` / 高光 `light` / 轮廓 `rim` 定义在 `renderers.js` 的 `PIECE_PALETTE`。
- 雨滴主体的渐变、月面陨石坑等原先硬编码的颜色已改为从 `palette` 取色，保证整体可统一调色。

## 3. 消除音效设计

每种元素一枚专属音色，全部走 Web Audio 程序化合成（`audio.js`）。音量区间 `0.009 ~ 0.02`，低于环境雨声的 `0.022` 基准增益，衰减偏慢，符合低沉克制的整体听感。

| 元素 | 音色 | 音高 | 时长 | 质感 | 与视觉的对应 |
|---|---|---|---|---|---|
| 雨滴 `rain` | 清脆钟声 + 水珠泛音 | 660Hz（E5）主音 + 880Hz 泛音 | 0.32s / 0.18s | 短促、颗粒、水感 | 青蓝的清澈冷冽 |
| 枯叶 `leaf` | 木质敲击双音 | 176Hz（F3）+ 221Hz（A3）轻微失谐 | 0.45s / 0.3s | 干燥、中频、木质感 | 赭褐的暖与凋零 |
| 乌云 `cloud` | 低频闷响长衰减 | 82Hz（E2）+ 58Hz（A♯1） | 0.75s / 0.55s | 低沉、压抑、如远雷 | 靛紫灰的厚重阴郁 |
| 残月 `moon` | 风铃泛音余音 | 660Hz（E5）+ 990Hz（B5） | 0.85s / 0.7s | 明亮、悠长、不散 | 暖米唯一的亮色 |
| 灰雾 `mist` | 气声噪声垫 | 800Hz 带通滤波噪声 | 0.5s | 弥散、无明确音高、呼吸感 | 无彩灰雾的迷离 |

### 统一规则

- **视觉越暗 → 音高越低**：cloud（82Hz）最低，moon（990Hz 泛音）最高。
- **冷色 → 清亮短促 / 气声**（rain 颗粒、mist 呼吸）；**暖色 → 木质中频**（leaf）。
- 连锁（combo）时统一叠加一个 98Hz 低音脉冲，不单独加亮音，保持克制。
- 所有音效音量低于游戏环境音效峰值，避免打断沉浸。

## 4. 文件改动清单

| 文件 | 改动内容 |
|---|---|
| `renderers.js` | 重写 `PIECE_PALETTE` 五种元素配色；雨滴渐变、月面陨石坑改为 palette 取色 |
| `audio.js` | 重写 `clear()` 按元素分发；新增 `tapRain / tapLeaf / tapCloud / tapMoon / tapMist / breath / noiseBuffer` 七种音色方法 |

未改动游戏规则与存档逻辑。

## 5. 调优指南

- **换色**：只改 `renderers.js` 的 `PIECE_PALETTE`（`base / dark / light / rim` 四色），无需动绘制逻辑。
- **换音**：只改 `audio.js` 中对应 `tap*` 方法的音高、时长、音量。
- 若想更"冷"：降低 `rain` 的红色通道；若想更"暖"：提升 `leaf` / `moon` 的亮度通道。
- 若觉得雾太"灰"导致难认：轻微提高 `mist` 的 `base` 亮度或加深 `cloud` 的 `dark`，拉开明度差。
