# 视觉与渲染优化分析

> 适用范围：`renderers.js`、`styles.css`、`assets.js`
> 原则：仅涉及视觉与渲染层面，游戏玩法、规则、存档逻辑零改动。

## 1. 升级背景

项目整体功能完善，但美术表现偏简单：背景仅为矩形建筑剪影加单一渐变，棋子缺乏材质与立体感，消除特效反馈弱，角色为线条火柴人，整体画面层次与沉浸感不足。

本次升级的目标：

1. 丰富材质细节（棋子、棋盘、按钮、面板的质感）。
2. 提升灯光与阴影质量（多层光源、内阴影、轮廓光、环境光）。
3. 增加复杂粒子与动效（雨幕、水花、灰烬、消除碎片、环绕星光）。
4. 引入风格化氛围（玻璃窗视角、情绪色调映射、后期暗角与滤色）。
5. 保证多设备性能（DPR 上限、粒子预算分级、预渲染精灵、`prefers-reduced-motion`）。

## 2. 升级前现状（问题清单）

| 层面 | 原实现 | 问题 |
|---|---|---|
| 背景 | 简单矩形建筑剪影 + 单一线性渐变天空 + 直线雨丝 | 无远近层次、无灯光细节、无材质 |
| 棋盘 | 棋子仅为"单一渐变 + 一个高光"；格子只有细描边 | 棋子扁平、缺立体感 |
| 消除特效 | 仅一圈扩散圆环 | 反馈弱，缺乏冲击力 |
| 角色 | 火柴人式黑色线条轮廓 | 无体积、无衣物、无场景 |
| 后期 | 单一暗角 + 简单 CSS `filter` | 层次与氛围感不足 |

## 3. 升级方案与实现细节

### 3.1 背景氛围系统（`ParticleSystem`，`renderers.js`）

**情绪色调映射（`SKY` 常量）**

五个情绪阶段各定义 `top / mid / bottom` 天空色、主光色（`glow`）与雾色（`fog`）。阶段切换时天空、光晕、雾气、雨水颜色统一迁移，营造"雨势随情绪渐弱/渐强"的叙事感。

**分层远景（绘制在背景画布 `#backdrop`）**

- 远山剪影：三层错落的贝塞尔山影，随视角缓慢漂移。
- 城市剪影：约 40 栋错落楼体（宽度/高度随机），窗口按概率点亮，灯光带相位差呼吸闪烁。
- 湿润地面：楼影倒映 + 斜向灯光倒影带 + 积水反光，雨夜质感。
- 星光：低频闪烁点，分布在低光区域。

**多层雨幕**

- 远 / 中 / 近三层雨丝（数量与透明度逐层递增，倾斜角随层增大）。
- 近景雨丝击中地面时产生水花涟漪（扩散圆环，渐隐）。
- 全局环境光（`glow` 色）叠加，模拟窗外灯火对雨幕的染色。

**前景玻璃水滴（绘制在特效画布 `#effects`，覆盖整个画面）**

- 一组雨痕：斜向细长轨迹，带高光边与末端水滴。
- 若干水滴：含反射高光，以低频微光照亮（`drops glow`）。
- 营造"从窗内看窗外"的沉浸视角，让棋盘界面也带窗玻璃质感。

**漂浮灰烬**

少量灰烬粒子（`ash`）在空气中缓慢漂移，增强静谧感；阶段切换时密度随之变化。

### 3.2 棋盘渲染（`BoardRenderer`，`renderers.js`）

**棋盘底板材质**

- 玻璃质感：中心径向渐变 + 边缘内阴影 + 顶部内发光。
- 每格：内嵌阴影 + 浅网格线 + 微弱的顶部高光，形成凹陷的玻璃格。

**程序化棋子（`paintPiece`）**

五种棋子全部程序化重绘，材质参数集中在 `PIECE_PALETTE`（基础色 / 深色 / 高光 / 轮廓光）：

- `rain` 雨滴：主体高光渐变 + 两道折射白斑 + 背部轮廓光。
- `leaf` 枯叶：中脉 + 放射叶脉 + 边缘卷曲高光。
- `cloud` 乌云：五团蓬松体积渐变 + 底部暗面 + 轮廓光。
- `moon` 残月：月面渐变 + 星点坑 + 月晕光晕。
- `mist` 灰雾：四团半透明雾 + 内侧微光尘点。

棋子通过 `buildSprites()` 预渲染为离屏画布精灵，运行时直接 `drawImage`，每帧代价极低。

**动效增强**

- 下落：使用 `easeBack` 弹性缓动，落地时带轻微回弹。
- 漂浮：静态棋子按相位做亚像素浮动，打破死板网格感。
- 选中：脉动发光边框 + 轻微上浮。
- 消除：白色闪光 + 双层扩散涟漪 + 十字星光 + 上升星尘，碎片残留短暂时间。
- 特殊棋：金色 / 银色星形徽记 + 旋转闪光 + 环绕微粒，始终可辨识。

### 3.3 角色渲染（`CharacterRenderer`，`renderers.js`）

**人物**

- 由线条火柴人升级为带体积感的填充角色：渐变躯干（外套、衣领）、多段式肢体（两层渐变模拟体积）、圆润头部 + 头发轮廓。
- 朝向窗外的轮廓光（`rim light`），与窗户冷光呼应。

**室内场景**

- 暗墙、窗框、窗格、窗帘、窗外月光。
- 窗玻璃上的雨痕、洒入地面的冷光块。
- 姿态系统（`POSES` / `setPose` / `play`）保留原有接口，UI 调用方式不变。

### 3.4 后期处理与 UI（`styles.css`）

- 背景照片层（`#backdropImage`）降为半透明纹理层（`opacity .5` + 饱和度/亮度/模糊滤镜），与程序化氛围叠合。
- 面板 / 卡片：多层玻璃渐变 + 内边框高光 + 顶部光带。
- 金属按钮：拉丝渐变 + 顶部高光 + hover 扫光（`::after` 背景位移动画）。
- 暗角：双层渐变（径向 + 纵向），随情绪阶段加深。
- 全局滤色：`body.stage-abyss` / `stage-melancholy` 等阶段滤镜 + 过渡动画。
- 标题、图标光晕，连锁脉冲强化。
- 响应式与 `prefers-reduced-motion` 降级。

## 4. 性能保障策略

| 手段 | 位置 | 说明 |
|---|---|---|
| DPR 上限 | `ParticleSystem.resize` / `BoardRenderer.resize` | `devicePixelRatio` 上限 2，避免高刷屏过载 |
| 粒子预算分级 | `detectQuality()` | 按 CPU 核心数 / 触屏设备 / DPR 分为 `high / medium / low` 三档，分别调整雨丝、水滴、碎片数量 |
| 棋子预渲染精灵 | `BoardRenderer.buildSprites()` | 64 枚棋子每帧仅 `drawImage`，消除逐帧路径绘制 |
| `prefers-reduced-motion` | 粒子系统 / CSS | 降级为低档并冻结部分动效；CSS 动画时长压缩到近似零 |
| 对象池 / 常量复用 | 各绘制循环 | 渐变、阴影参数按需生成，粒子数组按时长回收 |

## 5. 文件改动清单

| 文件 | 改动内容 |
|---|---|
| `renderers.js` | 重写 `ParticleSystem`（分层背景、三层雨幕、水花、玻璃水滴、灰烬）；重写 `BoardRenderer`（玻璃棋盘、程序化棋子、预渲染精灵、增强动效）；重写 `CharacterRenderer`（体积人物、室内场景）；新增 `SKY`、`PIECE_PALETTE`、`easeBack` 等常量与工具函数 |
| `styles.css` | 玻璃材质面板、金属按钮、氛围光、双层暗角、阶段滤色、响应式与动效降级 |
| `assets.js` | `pieces` 图集改为 `null`（统一使用程序化棋子）；背景照片保持加载，作为半透明纹理层 |

`index.html`、`board.js`、`game.js`、`ui.js`、`audio.js`、`music.js`、`data.js` 未改动，接口完全兼容。

## 6. 调优指南

以下常量可直接修改，无需改动渲染逻辑：

- `SKY`（`renderers.js`）：五个情绪阶段的天空色 / 光色 / 雾色。数组顺序与情绪阶段一一对应（雨 → 叶 → 云 → 月 → 雾）。
- `PIECE_PALETTE`（`renderers.js`）：各棋子 `base / dark / light / rim` 四色，调整配色即可换皮肤。
- 粒子数量：`ParticleSystem.rebuildRain` / `rebuildGlass` / `rebuildFog` / `rebuildAsh` 中的密度系数。
- 玻璃质感强度：`BoardRenderer.drawBoardBackground` 中的高光 / 阴影透明度。
- 照片纹理浓度：`styles.css` 中 `#backdropImage` 的 `opacity` 与 `filter`。
- 后期强度：`styles.css` 中 `.vignette` 的两层渐变与阶段透明度。

## 7. 未来扩展方向

- 消除时长为更长链式反应时加入"连击冲击波"与屏幕微震。
- 为每个章节引入独立的色调与天气主题（如"月"章加大月晕、"雾"章增浓雾气）。
- 角色增加简单循环动画（呼吸、眨眼），随阶段切换坐姿 / 站姿。
- 将棋盘底板与背景预渲染到离屏画布，仅在下落/消除时局部重绘，进一步降低移动端功耗。
- 引入 WebGL 粒子（可选渐进增强），在 `high` 档设备上使用 GPU 加速雨幕。
