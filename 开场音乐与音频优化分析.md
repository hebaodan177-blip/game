# 开场音乐

## 资源

- 文件：`music/opening.mp3`
- 来源：用户提供的 `Arvo Pärt-Spiegel Im Spiegel (1).mp3`
- 播放方式：原生 `HTMLAudioElement`，预加载、循环播放，目标音量为 0.22。

## 行为

1. 页面初始化后立即调用 `OpeningMusic.play()`，直接请求播放 MP3。
2. 若浏览器或站点已允许有声自动播放，音乐无需用户交互即可开始。
3. 若浏览器拒绝有声自动播放，脚本派发的点击事件不会被浏览器视为用户激活，不能用于绕过该限制；实现会在下一次真实点击、按键或触摸时自动重试播放。
4. 进入游戏或用户播放自选背景曲目时，开场音乐立即停止并回到开头；雨声、风声和心跳随后按原流程进入。

## 改动范围

- `audio.js`：移除程序化开场合成、音符表和下坠滑音，改为 MP3 播放控制器。
- `game.js`：进入游戏时停止开场 MP3，不再执行下坠过渡或延迟环境音。
- `music.js`：播放用户背景曲目前停止开场 MP3。
- `index.html`：刷新 `audio.js` 与 `game.js` 的版本参数。
